# Live per-project config — host-specific paths, do not commit.
# The *.example templates stay tracked.
*.agent.yml
services.compose.yml
*.services.compose.yml
settings.container.json
local-additional-context.md

# Test fixtures are not live config — keep them tracked (they ARE the suite).
!**/__tests__/fixtures/*.agent.yml
